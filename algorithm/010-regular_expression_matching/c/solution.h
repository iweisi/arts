bool isMatch(char* s, char* p) {
  do {
    if (*p >= 'a' && *p <= 'z') {
      //
    } else if (*p == '.') {
      //
    } else if (*p == '*') {
      //
    } else {
      break;
    }
  } while (*p++);

  return 1;
}